TO DO:
- Fix linting errors
- Create `setup-root.sh` and rename `setup.bash`'s extension

(add autoconnect) ssh-copy-id -f -i ~/.ssh/KEY_NAME ohrw@setonix.pawsey.org.au

-f flag is overwrite and -i flag is to add specific key

